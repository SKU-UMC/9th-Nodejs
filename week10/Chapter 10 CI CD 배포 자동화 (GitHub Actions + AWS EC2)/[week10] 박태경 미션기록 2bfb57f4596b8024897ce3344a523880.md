# [week10] 박태경/미션기록

![스크린샷 2025-12-04 오후 4.34.59.png](%E1%84%89%E1%85%B3%E1%84%8F%E1%85%B3%E1%84%85%E1%85%B5%E1%86%AB%E1%84%89%E1%85%A3%E1%86%BA_2025-12-04_%E1%84%8B%E1%85%A9%E1%84%92%E1%85%AE_4.34.59.png)

- deploy-main.yml
    
    ```yaml
    name: deploy-main
    
    on:
      push:
        branches:
          - main
      workflow_dispatch:
    
    jobs:
      deploy:
        runs-on: ubuntu-latest
    
        steps:
          - name: 레포지토리 체크아웃
            uses: actions/checkout@v4
    
          - name: SSH로 EC2 접속하여 배포 스크립트 실행
            uses: appleboy/ssh-action@v1.0.3
            with:
              host: ${{ secrets.EC2_HOST }}
              username: ubuntu
              key: ${{ secrets.EC2_SSH_KEY }}
              script: |
                cd /home/ubuntu/REPO
                git pull origin main
                npm install
                npm run build
                pm2 restart all
    ```
    
- EC2 환경에서 Google 로그인 불가 문제
    1. 원인
        
        Google OAuth Redirect URI가 로컬 기준으로 등록되어 있었음
        
        ex. `http://localhost:3000/oauth2/callback/google`
        
    2. 해결 방법
    ****Google Cloud Console에서 Redirect URI 수정